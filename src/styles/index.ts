.active-button {
  position: relative;
  border: 2px solid rgba(255, 255, 255, 0.3); /* Semi-transparent border */
  border-radius: 50%; /* Makes the border circular around icons */
  padding: 8px; /* Adds space between the border and the icon */
  transition: all 0.3s ease; /* Smooth transition for hover and active states */
}

.active-button:hover {
  border-color: rgba(255, 255, 255, 0.6); /* Slightly more visible on hover */
}

.active-button svg {
  display: block;
}
